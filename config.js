/**
 * Public GitHub `owner/repo` where **Releases** (installers) live.
 * Copy this site to that same public repo so the API `latest` matches your uploads.
 * Change if you use a different public repo name.
 */
window.__EVR_GITHUB_REPO__ = 'salArsalan4/evr-lite-site'
